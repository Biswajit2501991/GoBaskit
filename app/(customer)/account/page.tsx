import AccountPageClient from '@/components/Account/AccountPageClient';

export const metadata = {
  title: 'My Account — GoBaskit',
};

export default function AccountPage() {
  return <AccountPageClient />;
}
